export enum Roles {
  DEFAULT_ADMIN_ROLE = "DEFAULT_ADMIN_ROLE",
}

export enum Chains {
  RINKEBY = "rinkeby",
  BSC_TEST = "bscTestnet",
}

export enum ChainEnv {
  "rinkeby" = "RINKEBY",
  "bscTestnet" = "BSC_TEST",
}
