export enum Roles {
  DEFAULT_ADMIN_ROLE = "DEFAULT_ADMIN_ROLE",
}

export enum Chains {
  RINKEBY = "rinkeby",
  BSC_TEST = "bscTestnet",
  HARDHAT = "hardhat",
}

export enum ChainEnv {
  "rinkeby" = "RINKEBY",
  "bscTestnet" = "BSC_TEST",
  "hardhat" = "HARDHAT",

}
